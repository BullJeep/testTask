
&НаКлиенте
Процедура Сравнить(Команда)
	СравнитьНаСервере();
КонецПроцедуры

&НаСервере
Процедура СравнитьНаСервере()
	ОбъектФормы = РеквизитФормыВЗначение("Объект");
	Результат = ОбъектФормы.Сравнить(ЭтотОбъект.Версия1, ЭтотОбъект.Версия2); 
	Сообщение = Новый СообщениеПользователю;
	Сообщение.Текст = "Версия 1 "+?(Результат=0,"равна", ?(Результат<0,"больше","меньше"))+" Версии 2";
	Сообщение.Сообщить();
КонецПроцедуры
